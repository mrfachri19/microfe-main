const base = 'Diarium | ';
const title = {
  HOME: base + 'Home',
  LOGIN: base + 'Login',
  PERMOHONAN: base + 'Permohonan',
  PROFILE: base + 'Profil',
  SEARCH: base + 'Pencarian',
  TASK: base + 'Tugas',
};

export default title;
